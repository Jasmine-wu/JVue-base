<template>
  <div></div>
</template>

<script>
// 1.子组件内能不能改变父组件传递过来的数据，可以，但vue不推荐
// 因为子组件内部修改父组件传递的数据，并不会通知父组件，造成子父组件数据不一致。
// 单向数据流：从父向子传递的数据流
// 2.vue props里的属性本身就是只读的，不允许修改

// 问题：既然子组件内不能修改父组件传递过来的数据？那子组件如何和父组件通信？
// 解决：在子组件触发父组件自定方法
// 分析：子组件向父组件通信：
// 要知道操作的是哪个子组件：父组件传入子组件index属性
// 要知道触发哪个方法（砍价），父组件绑定子组件方法,处理放到父组件中
export default {};
</script>